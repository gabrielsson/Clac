<?xml version="1.0" encoding="utf-8"?>

<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:orientation="horizontal"
    android:layout_gravity="start"
    android:gravity="start"
    android:background="@drawable/background">

    <TextView
        android:id="@+id/arr_datum"
        android:text="Alltid, framförallt sent på natten"
        android:textStyle="bold"
        android:layout_marginEnd="50dp"
        android:layout_marginRight="50dp"
        android:textColor="?attr/textColorPrimary"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:gravity="start" />

    <TextView
        android:id="@+id/arr_namn"
        android:text="Palatset @ Hos #15"
        android:textStyle="bold"
        android:textColor="?attr/textColorPrimary"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_toEndOf="@id/arr_datum"
        android:layout_toRightOf="@id/arr_datum" />

    <TextView
        android:id="@+id/arr_deltagare"
        android:text="#15, #18, #40, #44"
        android:textStyle="italic"
        android:layout_width="180dp"
        android:layout_height="wrap_content"
        android:layout_below="@id/arr_datum" />

    <TextView
        android:id="@+id/arr_hetsade"
        android:text="| Hets: #62, #68, #71, #71sMamma"
        android:textStyle="italic"
        android:layout_width="180dp"
        android:layout_height="wrap_content"
        android:layout_below="@id/arr_deltagare" />

    <View
        android:layout_height="1dp"
        android:layout_width="fill_parent"
        android:background="@color/gray" />

</RelativeLayout>
